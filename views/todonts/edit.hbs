<h1>Edit {{todonts.description}}</h1>

<form method="POST" action="/todonts/{{id}}?_method=PUT">
	<div>
		<label for="description">Description: </label>
		<input type="text" name="description" value="{{todonts.description}}" />
	</div>

	<div>
		<label for="urgent">Urgent: </label>
		{{#if todonts.urgent}}
			<span>Yes</span><input type="radio" name="urgent" value="true" checked />
			<span>No</span><input type="radio" name="urgent" value="false" />
		{{else}}
			<span>Yes</span><input type="radio" name="urgent" value="true" checked />
			<span>No</span><input type="radio" name="urgent" value="false" />
		{{/if}}
	</div>

	<button>UPDATE TODO</button>
</form>